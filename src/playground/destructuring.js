/**
 * obj destructuring
 * destructuring objects made simple with the new syntax
 **/

/*
const book = {
    title: 'Ego is the enemy',
    author: 'Jordan Cash',
    publisher: {
        name: 'Penguin'
    }
};
const { title, author } = book;
console.log(`books title is ${title} and the author is ${author}`);

const { name: publisherName = 'Self-published' } = book.publisher;
console.log(`${publisherName}`); */

/**
 * array destructuring
 **/

/* const coffee = ['Coffee (hot)', '$2.0', '$2.5', '$3.0'];
const [coffeeName, , mediumSize] = coffee;

console.log(`${coffeeName} costs ${mediumSize} for the medium size`); */







